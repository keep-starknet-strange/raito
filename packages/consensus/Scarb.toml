[package]
name = "consensus"
version = "0.1.0"
edition = "2024_07"

[dependencies]
utils = { path = "../utils" }
raito_logging = { path = "../logging" }

[dev-dependencies]
cairo_test.workspace = true

[scripts]
# TODO: cairo lint
lint = "scarb fmt"

[features]
default = ["log_level_none"]
log_level_none = []
log_level_trace = []
log_level_debug = []
