#[cfg(test)]
mod tests {
    use logging::ABC;
    use logging::LOG_LEVEL_DEBUG;
    #[test]
    fn test_log_level_debug() {
        // log!("DEBUG", "log! works");
    }

    #[test]
    fn test_log_level_trace() {
        // log!("DEBUG", "log! works");
    }
}
